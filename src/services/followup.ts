export type FollowupReviewStatus = 'approved' | 'edited' | 'rejected'
export interface FollowupPending { id:string; case_id:string; case_summary:string; access_barriers:string[]; case_status:string; suggested_action:string; action_details:string; follow_up_at:string; requires_human_review:boolean; created_at:string }
export interface FollowupReviewRequest { followup_id:string; status:FollowupReviewStatus; reviewed_by:string; edited_action?:string|null }
export async function fetchPendingFollowups():Promise<FollowupPending[]>{const response=await fetch('/api/followup/pending');const raw=await response.text();if(!response.ok)throw new Error(`Error al listar seguimientos (${response.status})`);if(!raw.trim())return[];const data=JSON.parse(raw) as unknown;if(Array.isArray(data))return data as FollowupPending[];if(data&&typeof data==='object'&&Array.isArray((data as {data?:unknown}).data))return (data as {data:FollowupPending[]}).data;return[]}
export async function reviewFollowup(payload:FollowupReviewRequest){const response=await fetch('/api/followup/review',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const data=await response.json();if(!response.ok)throw new Error('No se pudo revisar el seguimiento.');return data as {ok:boolean;error?:string}}
export function actionLabel(action:string){return({confirm_attention:'Confirmar atención',send_reminder:'Enviar recordatorio',human_review:'Revisión humana',close_case:'Cerrar caso',reopen_case:'Reabrir caso'} as Record<string,string>)[action]||action}
